export const initialCalendarState = {
  checkIn: null,
  checkOut: null,
  hoveredDate: null,
};

export const initialPriceState = {
  minPrice: null,
  maxPrice: null,
};

export const initialPersonnelState = {
  ADULT: 0,
  CHILD: 0,
  INFANT: 0,
};
